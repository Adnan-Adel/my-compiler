#include "../std_types.h"
#include <stdio.h>
#include <stdlib.h>

uint32_t str_length(char* str);

/* Compares two strings and returns a flag  1-> Strings are the same.   0-> Not the same */
char compare_str(char* str1, char* str2);